const db = require("../../data/dbConfig");

// todo: getAll, getById, update

const insert = (item) => {
  return null;
};
const remove = (id) => {
  return null;
};

module.exports = {
  insert,
  remove,
};
